# 概念

```cpp
#include<stdio.h>

int Max(int a, int b){
    return a > b ? a : b;
}

int main(){
    int (*p)(int, int); // 函数指针
    int a = 1, b = 2;
    p = Max; // 函数指针指向函数
    printf("%d\n", (*p)(a, b));
    return 0;
}
```

输出

```
2
```